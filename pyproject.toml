[project]
name = "github-analyzer"
version = "0.1.0"
description = ""
authors = [
    {name = "Louis Lemoine",email = "louislemoine4@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests (>=2.34.1,<3.0.0)",
    "rich (>=15.0.0,<16.0.0)",
    "typer (>=0.25.1,<0.26.0)",
    "python-dotenv (>=1.2.2,<2.0.0)"
]

[tool.poetry]
packages = [{include = "github_analyzer", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
